# 移除二维投影拒绝后的全量预检（2026-09-12）

用户已明确允许移除二维投影交集作为拒绝条件；此前等待范围确认的记录是历史状态。
本次实际重跑87个源，每源必须两个相位同时通过才算通过。

**69源通过，18源拒绝；原58个失败源中恢复40个。** 原29个通过源全部仍通过。

剩余源：8、9、11、12、14、15、18、32、44、48、50、51、55、60、61、62、63、68。

- 10源：刹停/等待阶段机械臂或持物球与瞬时抽屉代理冲突。
- 7源：最高2mm区域没有通过扫掠几何的候选。
- 1源（60）：原分析抓取边界错误。另用昨日已记录的人工边界重跑，持物区间
  能识别，但仍因最高点几何无候选而拒绝；采用该修正也仍是18源拒绝。

为何不是按旧分类估计的13个：9、11、12、14、15此前在二维条件处提前停止，
本轮进入后续检查后也被抽屉几何代理拒绝。几何拒绝仍不等于实测物理不可能。

## 实验范围

基于实验分支25bb044，将源码复制到独立runtime，仅删除两处二维拒绝：
`opening_wait_candidates`返回原eligible，`plan_visual.clear`不再因mask交集拒绝。
[精确补丁](no-projection.patch) 和 [执行脚本](scripts/no-projection-preflight.py) 已保存。
其余已有阶段、峰值、抓取、时钟及抽屉几何检查保留，几何参数未修改。
没有增加或宣称完成原管线未覆盖的全轨迹碰撞检查。

产物根为 `/home/coder/share/drawer-58-no-projection-20260912`，包含 config.json、
runtime/src、work逐源计划/feasibility、错误日志和完整preflight.json。
PYTHONPATH使用该runtime/src及已有robo-visualize/src，3个独立进程各处理不同源。
源60已校正分析的对照单独使用manual60-work，不覆盖基线全量结果。

[汇总](no-projection-summary.json) · [全部逐源结果](no-projection-preflight.json)

这是回答拒绝数量的规划消融实验，未渲染、未finalize；69源不表示138条视频已验收。
去掉二维规划拒绝后，渲染端的遮挡合成及相应验证仍需后续实现。本次生产源码和
main均未修改，实验计划使用自身producer指纹，不能作为旧生产代码的计划复用。

提交前审查了精确差异、87源唯一性、两变体通过要求、原29源无回退及源60独立对照。
