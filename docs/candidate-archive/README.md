# from-scratch 候选实现交接

## 归档原因与范围

2026-09-10，用户确认另一个分支已完整实现任务，要求停止本实现，将当前代码及未解决问题保存为候选分支。Git 不允许分支名包含空格，因此使用 `from-scratch`。本归档不修改、合并或覆盖 `main`，也不将本候选宣称为正式交付。

归档前版本为 `b054795`。本次归档另包含尚未提交的两项修复：重叠方块候选的完整原位恢复，以及覆盖参考对非机械臂运动的过滤。审查去除了重复的原位范围膨胀计算；完整测试 **108 passed**。测试通过不等于真实数据全部验收通过。

## 未解决或尚未验收的问题

1. **Episode 1 的物理调度无解，尚未定位根因。** 视觉释放已修正为最早完整确认的证据，交互检查通过；`b054795` 的 `plan_joints` 在严格检查及允许精确原始配对接触边之后，仍抛出 `NoSafeSchedule: no safe schedule preserving source trajectories and priority`。日志：`release-b054795/episode_001.log`。不能通过放宽任意碰撞检查或伪造成功规避。需要检查抽屉开启/关闭、等待/撤回边界与原始接触边之间的约束是否相容。

2. **Episode 49 的修复仅通过交互复验，尚未完成最终导出。** 旧覆盖参考把操作人员伸入画面的前臂误当左机械臂，造成 35 帧假缺失；另有 3 帧模糊线缆。增加足够深色硬件支持后，`release-hardware/episode_049` 交互检查通过，但该结果尚未进入最终数据输出，仍需规划、渲染、逐帧数值检查与真实画面验收。旧 `release-b054795` 的失败记录仍保留，不能与修复后的缓存混淆。

3. **Episode 0 的青色原位残留修复尚未完成最终实片验收。** 两个自动候选覆盖同一方块的不同表面，旧恢复区域只覆盖选中的表面，导致残留。当前代码合并重叠候选的原位范围，并在抓取前曝光匹配、清洁背景恢复和原位审计中使用完整范围；回归测试通过。`origin-footprint0` 是中间试渲染，运行期间发生过后续源码修改，不能将其 producer fingerprint 视作最终冻结版本的可靠证明。需要用归档 commit 的不可变源码重新渲染、与源帧对照，并更新输出收据与哈希验收。

4. **整体视觉质量仍有局限。** 曾发现机械臂掩码包含柜体/抽屉/另一只夹爪、遗漏连接环和夹爪尖端、释放后对象掩码跟随手、场景矩形亮度接缝。已有自动恢复、对象身份/释放确认、场景归属、真实背景轮廓补全与弱对比度透明度修复，但仍可见轻微轮廓、阴影及曝光接缝。关键帧抽检不代表逐像素或全视频无缺陷；未验收条目不能视为已通过。

5. **87 条数据尚未完整交付。** 最新候选批次只有 18 条生成完成并通过数值/腕部视频等检查，4 条完成关键帧视觉审核；1、49 为实际失败；18、19、21、22 因用户本次停止请求被中断。其余未处理。详细编号和状态见 `batch-status.json`。历史批次的“78 complete”不能作为最新版本验收结果。

6. **正式发布步骤未执行。** 尚未完成全部 87 条的元数据/统计汇总、最终全数据验证、全部视觉验收和公开 HF 上传。目标 `Shiki42/piperx-put-cube-in-drawer-retime` 本任务未创建或上传；此前只有独立预览仓库。归档的 prepare/upload 脚本是未执行的准备工具，不能当作交付证明。

## 已保留的有效成果与约束

- 原始 87 条共 54,463 帧；trim 已验证为 53,796 帧，删除 667 帧开头静止段。
- 所有原始尾段不足两秒，trim 保留原始尾部；retime 添加 60 帧显式标记的合成边界重复帧。
- 交互推断仅使用视频；state/action 用于裁剪、调度、物理检查和独立左右源时钟映射。
- 几何保持 RoboVisualize 提供的 0.49 m 基座间距、平行 +X 和 URDF 安装旋转。
- 原始录制已有的建模接触只允许精确相邻原始双臂配对边重放，必须保留 ledger；不得宣称绝对无碰撞。
- 对旧输出的复用仅在时间线、分割数组、配准完全相同，并证明规划/渲染实现未变化时进行；收据保留旧 renderer fingerprint 和明确的 `reuse_verification`。

## 代码、工件与后台状态

远端代码目录：`/home/coder/share/real-robot-data-retime`（Coder A）。工件根目录：`/home/coder/share/retime-interaction-20260909`。

主要工件目录：

- `drawer`、`drawer-trimmed`：原始和已验证 trim 数据。
- `drawer-retimed-b054795`、`analysis-b054795`：最新候选输出与分析缓存。
- `release-b054795`、`numeric-b054795`、`visual-review-b054795`：生成状态、数值验收和视觉记录。
- `release-hardware/episode_049`：通过最新硬件参考过滤的 49 号缓存。
- `release-earliest/episode_001`：已修正释放的 1 号缓存，但规划仍失败。
- `release-source-inventory.json`：525 个 raw 文件、354 个 trim 文件的源清单与哈希。
- `validation-*`、`experiments/*/src`：历史不可变源码快照；不要重标旧 producer。

`run-scripts/` 保存关键历史批处理与验收脚本，其路径及版本是归档现场值，**不是新的默认产品入口**。完整运行轨迹见 `progress-history.md`，最新状态以本文件和 `batch-status.json` 为准。

本次停止设置各队列 STOP 文件，并对 18、19、21、22 的在途子进程发送 SIGTERM，监督进程已记录退出码 -15；这些状态另标为 `interrupted`。相关生成、重渲染、验收工作进程已退出。不要自动恢复这些队列，也不要自动上传候选数据。

本地 `/Users/shuyuan/Documents/real-robot-data-retime` 的 `main` 已由另一个任务修改。本任务仅同步候选分支引用，不覆盖该 `main`。
